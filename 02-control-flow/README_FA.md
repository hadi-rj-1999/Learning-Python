# کنترل جریان در پایتون - مواد آموزشی

این پوشه شامل دستورات کنترل جریان پایتون برای منطق برنامه و تصمیم‌گیری است.

## 📁 بررسی فایل‌ها

### [if_else.py](if_else.py)
- دستورات پایه `if`
- ساختارهای `if-else`
- زنجیره‌های `if-elif-else`
- عملگرهای مقایسه‌ای (`==`, `!=`, `>`, `<`, `>=`, `<=`)
- عملگرهای منطقی (`and`, `or`, `not`)
- دستورات if تو در تو
- عملگر سه‌تایی (عبارات شرطی)
- آزمایش عضویت با `in` و `not in`

### [loops_for.py](loops_for.py)
- حلقه‌های `for` با iterableهای مختلف:
  - لیست‌ها، تاپل‌ها، رشته‌ها
  - دیکشنری‌ها (کلیدها، مقادیر، آیتم‌ها)
  - مجموعه‌ها و بازه‌ها
- تابع `range()` با پارامترهای مختلف
- دستورات `break` و `continue`
- بند `else` در حلقه‌های for
- حلقه‌های for تو در تو
- `enumerate()` برای جفت‌های index-value
- `zip()` برای چندین دنباله
- List comprehensions

### [loops_while.py](loops_while.py)
- حلقه‌های `while` پایه
- حلقه‌های `while` با ورودی کاربر
- `break` و `continue` در حلقه‌های while
- بند `else` در حلقه‌های while
- حلقه‌های while تو در تو
- مثال‌های عملی:
  - بازی حدس عدد
  - دنباله فیبوناچی
  - محاسبه فاکتوریل
  - اعتبارسنجی رمز عبور
  - سیستم‌های منو

### [break_continue.py](break_continue.py)
- استفاده دقیق از دستور `break`
- استفاده دقیق از دستور `continue`
- break/continue در هر دو حلقه for و while
- break/continue در حلقه‌های تو در تو
- الگوهای عملی:
  - خروج زودهنگام برای کارایی
  - رد کردن عملیات پرهزینه
  - حلقه‌های اعتبارسنجی ورودی
- تعامل با بندهای `else` حلقه

## 🎯 اهداف یادگیری
- تصمیم‌گیری با دستورات شرطی
- تکرار روی دنباله‌ها با حلقه‌ها
- کنترل اجرای حلقه با break/continue
- مدیریت سناریوهای مختلف حلقه
- نوشتن کنترل جریان کارا و خوانا

## 💡 نکات مهم
- برای شرایط چندگانه از `elif` به جای if تو در تو استفاده کنید
- وقتی تعداد تکرارها از قبل مشخص است از حلقه for استفاده کنید
- برای تکرارهای نامعلوم از حلقه while استفاده کنید
- با حلقه‌های while بی‌نهایت مراقب باشید
- وقتی به اندیس نیاز دارید از `enumerate()` استفاده کنید
- List comprehensions برای تبدیل‌های ساده پایتونیک‌تر هستند