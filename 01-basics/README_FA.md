# مبانی پایتون - مواد آموزشی

این پوشه شامل مفاهیم پایه‌ای برنامه‌نویسی پایتون برای افراد تازه‌کار است.

## 📁 بررسی فایل‌ها

### [variables.py](variables.py)
- تعریف و مقداردهی متغیرها
- نوع‌دهی پویا در پایتون
- تبدیل نوع (str, int, float)
- مقداردهی چندتایی به متغیرها
- قوانین و قراردادهای نام‌گذاری متغیرها

### [data_types.py](data_types.py)
- انواع داده‌های built-in پایتون:
  - نوع متنی: `str`
  - انواع عددی: `int`, `float`, `complex`
  - انواع ترتیبی: `list`, `tuple`, `range`
  - نوع نگاشتی: `dict`
  - انواع مجموعه: `set`, `frozenset`
  - نوع بولین: `bool`
  - انواع باینری: `bytes`, `bytearray`, `memoryview`
- بررسی نوع با تابع `type()`

### [input_output.py](input_output.py)
- خروجی پایه با `print()`
- دریافت ورودی کاربر با `input()`
- تبدیل و تغییر نوع ورودی
- دریافت چندین ورودی با `split()` و `map()`
- خروجی فرمت‌شده با f-strings و `format()`
- خروجی چندخطی با triple quotes

## 🎯 اهداف یادگیری
- درک سیستم متغیرها در پایتون
- تسلط بر انواع داده مختلف و کاربردهای آنها
- مدیریت مؤثر ورودی کاربر و نمایش خروجی
- اعمال تکنیک‌های مناسب تبدیل نوع
- نوشتن کد پایتون تمیز و خوانا

## 💡 نکات مهم
- پایتون از نوع‌دهی پویا استفاده می‌کند - متغیرها می‌توانند نوع خود را تغییر دهند
- `input()` همیشه یک رشته برمی‌گرداند
- از نام‌های توصیفی برای متغیرها با فرمت snake_case استفاده کنید
- با عملیات مختلف روی انواع داده تمرین کنید