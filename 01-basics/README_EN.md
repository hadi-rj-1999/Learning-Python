# Python Basics - Learning Materials

This directory contains fundamental Python programming concepts for beginners.

## 📁 Files Overview

### [variables.py](variables.py)
- Variable declaration and assignment
- Dynamic typing in Python
- Type casting (str, int, float)
- Multiple variable assignment
- Variable naming rules and conventions

### [data_types.py](data_types.py)
- Python built-in data types:
  - Text Type: `str`
  - Numeric Types: `int`, `float`, `complex`
  - Sequence Types: `list`, `tuple`, `range`
  - Mapping Type: `dict`
  - Set Types: `set`, `frozenset`
  - Boolean Type: `bool`
  - Binary Types: `bytes`, `bytearray`, `memoryview`
- Type checking with `type()`

### [input_output.py](input_output.py)
- Basic output with `print()`
- User input with `input()`
- Input conversion and casting
- Multiple inputs with `split()` and `map()`
- Formatted output with f-strings and `format()`
- Multiline output with triple quotes

## 🎯 Learning Objectives
- Understand Python variable system
- Master different data types and their uses
- Handle user input and display output effectively
- Apply proper type conversion techniques
- Write clean and readable Python code

## 💡 Tips
- Python uses dynamic typing - variables can change types
- `input()` always returns a string
- Use descriptive variable names with snake_case
- Practice with different data type operations